
# dryjs Development Workflow

- Every feature = new branch from master
- Read all guardrail files first in every session
- Run `npm test` after every module
- Use Vitest + Vite for testing
- Commit only when tests pass
